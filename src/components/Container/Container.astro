---
import styles from "./Container.module.scss"

const { as = "div", size = "medium" } = Astro.props

const Element = as
---

<Element class:list={[styles.el, styles[`el_${size}`]]}>
  <slot />
</Element>
